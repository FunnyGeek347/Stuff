my_file = open("newfile.txt", "a")
def timestable(num):
  for i in range(1,13):
    my_file.write(str(i)+"x"+str(num)+"="+str(num*i)+"\n")
    
for i in range(2,12):
  timestable(i+1)
my_file.close()
